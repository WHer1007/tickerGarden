# Website workspace instructions

This directory is the TickerGarden V2 web surface. Keep the page focused on protocol status and future V2 integration boundaries until the canonical ABI, execution invariants, and runtime services are implemented.

- Preserve the existing Vite, React, Sites worker, hosting configuration, and package lock.
- Use the existing brand assets under `public/assets`.
- Do not add market actions, wallet flows, or product claims to the scaffold page.
- Keep V2 identifiers aligned with the repository execution specification.
- Run `npm run build` and `npm run test:sites` after changes.
