import {markets,money,escapeHtml as esc,estimate} from './data.js';
const nav=[['home','首页'],['trade','交易'],['markets','市场'],['create','创建代币'],['analytics','数据统计'],['rewards','奖励']];
function readStored(key,fallback){try{return JSON.parse(localStorage.getItem(key))??fallback;}catch{return fallback;}}
const state={wallet:false,filter:'all',query:'',sort:'volume',selected:'nvcat',side:'buy',period:'1D',range:'7D',rewardTab:'stake',favorites:new Set(Array.isArray(readStored('tg-v2-favorites',[]))?readStored('tg-v2-favorites',[]):[])};
const $=s=>document.querySelector(s);const token=()=>markets.find(m=>m.id===state.selected)||markets[0];
const icon=m=>`<span class="token-icon" style="--token:${m.color}">${m.icon}</span>`;
const change=m=>`<span class="${m.change>=0?'positive':'negative'}">${m.change>=0?'+':''}${m.change.toFixed(2)}%</span>`;
function spark(m,large=false){const pts=m.points.map((v,i)=>`${i*100/(m.points.length-1)},${100-v}`).join(' ');return `<svg class="${large?'large-chart':'spark'}" viewBox="0 0 100 100" preserveAspectRatio="none" role="img" aria-label="${esc(m.symbol)} 演示价格趋势"><polyline points="${pts}" fill="none" stroke="${m.change>=0?'#408561':'#c77064'}" stroke-width="${large?'.45':'2'}" vector-effect="non-scaling-stroke"/></svg>`;}
function badge(m){return `<span class="badge ${m.progress===100?'bloom':''}">${m.progress===100?'✳ In Bloom':'◌ Growing · '+m.progress+'%'}</span>`;}
function heading(kicker,title,desc,extra=''){return `<div class="page-heading"><div><div class="eyebrow">${kicker}</div><h1>${title}</h1><p>${desc}</p></div>${extra}</div>`;}
function stats(items){return `<div class="stats">${items.map(([label,value,note])=>`<div><span class="muted">${label}</span><strong>${value}</strong><small>${note}</small></div>`).join('')}</div>`;}
function card(m){return `<a class="market-card" href="#trade/${m.id}"><div class="card-top">${icon(m)}${badge(m)}</div><h3>${m.name}</h3><div class="muted">$${m.symbol}<span class="base-tag">${m.base} 生态</span></div><div class="card-price"><b>${money(m.cap)}</b>${change(m)}</div>${spark(m)}<div class="card-bottom"><span>24h 交易量 <b>${money(m.volume)}</b></span><span>↗</span></div></a>`;}
function table(list){return `<div class="table-wrap"><table><thead><tr><th>代币 / 关联 STOCK</th><th>价格 (USD 参考)</th><th>24h 涨跌</th><th>市值</th><th>24h 交易量</th><th>趋势</th><th>市场阶段</th><th><span class="sr-only">收藏</span></th></tr></thead><tbody>${list.map(m=>`<tr><td><a class="token-cell" href="#trade/${m.id}">${icon(m)}<span><b>${m.name}</b><small>$${m.symbol} <span>· ${m.base}</span></small></span></a></td><td>$${m.price.toFixed(7)}</td><td>${change(m)}</td><td>${money(m.cap)}</td><td>${money(m.volume)}</td><td>${spark(m)}</td><td>${badge(m)}</td><td><button class="star" data-favorite="${m.id}" aria-label="收藏 ${m.symbol}" aria-pressed="${state.favorites.has(m.id)}">${state.favorites.has(m.id)?'★':'☆'}</button></td></tr>`).join('')}</tbody></table>${list.length?'':'<div class="empty">没有找到匹配的市场。试试其他名称，或切换筛选条件。</div>'}</div>`;}
function home(){return `<section class="hero"><div class="hero-copy"><div class="eyebrow"><span class="live-dot"></span> CULTURE TAKES ROOT.</div><h1>让灵感发芽。<br>让文化<span class="serif">生长。</span></h1><p>发现下一个有生命力的 Meme。<br>连接股票社区，交易你相信的文化。</p><div class="button-row"><a class="button primary" href="#markets">探索市场 <span>↗</span></a><a class="button outline" href="#create">种下你的代币 <span>＋</span></a></div><div class="hero-note"><span class="mini-seed">✳</span> 社区驱动 <i></i> 链上发行 <i></i> 永久锁定初始流动性</div></div><div class="garden-art" aria-label="全新绘制的市场花园图案"><div class="orbit orbit-one"></div><div class="orbit orbit-two"></div><div class="art-grid"></div><div class="flower flower-main"><span>✳</span></div><div class="flower flower-purple"><span>✿</span></div><div class="flower flower-orange"><span>✺</span></div><div class="stem stem-one"></div><div class="stem stem-two"></div><div class="stem stem-three"></div><div class="leaf leaf-one"></div><div class="leaf leaf-two"></div><div class="floating-label label-one"><i class="live-dot"></i> A NEW MARKET IS GROWING</div><div class="floating-label label-two">✳ CULTIVATED BY COMMUNITY <span>↗</span></div><div class="art-caption">THE ONCHAIN CULTURE GARDEN<br><span>EST. 2026 — ALWAYS GROWING</span></div></div></section>${stats([['24h 交易量','$269.6K','↗ 演示市场概览'],['市场总市值','$1.50M','6 个社区，共同生长'],['绽放市场','02 / 06','初始流动性永久锁定'],['社区持有人','4,655','演示持有人统计']])}<section><div class="section-heading"><div><div class="eyebrow">GROWING TOGETHER</div><h2>花园里的热门发现 <span class="tiny-pill">HOT</span></h2></div><a href="#markets">查看全部市场 ↗</a></div><div class="cards">${markets.slice(0,3).map(card).join('')}</div></section><section class="how"><div><div class="eyebrow">FROM AN IDEA TO A COMMUNITY</div><h2>一个灵感，<br>无限生长的可能。</h2><a href="#create">开始创建 ↗</a></div>${[['01','种下灵感','选择关联 STOCK，创建属于社区的 Meme。'],['02','一起生长','通过曲线交易发现价格，达到条件后自动绽放。'],['03','分享收获','绽放后分配对应 STOCK，分享实际产生的手续费。']].map(([n,t,d])=>`<article><span class="step-number">${n}</span><h3>${t}</h3><p>${d}</p></article>`).join('')}</section>`;}
function marketPage(){let list=markets.filter(m=>(state.filter==='all'||state.filter==='bloom'&&m.progress===100||state.filter==='growing'&&m.progress<100||state.filter==='favorites'&&state.favorites.has(m.id))&&`${m.name} ${m.symbol} ${m.base}`.toLowerCase().includes(state.query.toLowerCase()));list.sort((a,b)=>state.sort==='change'?b.change-a.change:state.sort==='cap'?b.cap-a.cap:b.volume-a.volume);return `${heading('EXPLORE THE GARDEN','发现值得生长的社区。','每一个 Ticker，都是一种共同相信的文化。','<a class="button primary" href="#create">＋ 创建代币</a>')}${stats([['市场总数','6','所有演示市场'],['24h 交易量','$269.6K','以 USD 估值展示'],['In Bloom','2','已开放 STOCK 分配'],['正在生长','4','曲线交易阶段']])}<div class="panel"><div class="toolbar"><div class="tabs">${[['all','全部市场'],['bloom','✳ In Bloom'],['growing','正在生长'],['favorites','☆ 自选']].map(([id,t])=>`<button data-filter="${id}" class="${state.filter===id?'active':''}">${t}</button>`).join('')}</div><div class="table-controls"><input id="market-search" aria-label="搜索市场" placeholder="搜索代币或 STOCK…" value="${esc(state.query)}"><select id="market-sort" aria-label="市场排序"><option value="volume" ${state.sort==='volume'?'selected':''}>按交易量</option><option value="change" ${state.sort==='change'?'selected':''}>按涨幅</option><option value="cap" ${state.sort==='cap'?'selected':''}>按市值</option></select></div></div>${table(list)}</div><p class="footnote">关联 STOCK 是市场的质押基础资产，不代表 Meme 可赎回股票，或跟踪股票价格。</p>`;}
function chart(m){return `<div class="chart-area"><div class="chart-grid"></div><div class="y-axis"><span>0.0000500</span><span>0.0000400</span><span>0.0000300</span><span>0.0000200</span></div><div class="candles">${Array.from({length:48},(_,i)=>{const v=24+i*.75+Math.sin(i*1.8+(state.period==='1D'?0:2))*8;return `<i class="candle ${i%3===0?'down':''}" style="--bottom:${v}%;--height:${4+i%6}%"></i>`;}).join('')}</div><div class="chart-watermark">TickerGarden<span>DEMO CHART · ${state.period}</span></div><div class="volume-bars">${Array.from({length:48},(_,i)=>`<i style="height:${20+(i*37)%80}%"></i>`).join('')}</div></div><div class="x-axis"><span>00:00</span><span>06:00</span><span>12:00</span><span>18:00</span><span>23:59</span></div>`;}
function trade(){const m=token();return `${heading('TRADE YOUR CONVICTION','交易，让共识生长。','市场价格与行情为交互演示，不构成实时报价。')}<div class="trade-grid"><div><div class="panel"><div class="trade-token"><div class="token-cell">${icon(m)}<div><h2>${m.name}</h2><span class="muted">$${m.symbol} · ${m.base} 生态</span></div></div><select id="trade-market" aria-label="选择交易市场">${markets.map(x=>`<option value="${x.id}" ${x.id===m.id?'selected':''}>${x.symbol}</option>`).join('')}</select>${badge(m)}</div><div class="price-line"><strong>$${m.price.toFixed(7)}</strong>${change(m)}<span class="muted">24h</span><div class="price-stat">市值 <b>${money(m.cap)}</b></div><div class="price-stat">24h 交易量 <b>${money(m.volume)}</b></div></div><div class="chart-toolbar"><div class="tabs">${['1H','4H','1D','1W'].map(p=>`<button data-period="${p}" class="${p===state.period?'active':''}">${p}</button>`).join('')}</div><span class="muted">价格 / USD · 示例蜡烛图</span></div>${chart(m)}</div><div class="panel recent"><div class="section-heading"><h3>最近交易</h3><span class="muted">演示记录</span></div><div class="table-wrap"><table><thead><tr><th>时间</th><th>方向</th><th>数量</th><th>交易者</th></tr></thead><tbody>${[1,3,5,8].map((n,i)=>`<tr><td>${n} 分钟前</td><td class="${i%3?'positive':'negative'}">${i%3?'买入':'卖出'}</td><td>${(12400+n*2870).toLocaleString()} ${m.symbol}</td><td class="muted">演示账户 ${i+1}</td></tr>`).join('')}</tbody></table></div></div></div><aside><div class="panel order-panel"><div class="segmented"><button data-side="buy" class="${state.side==='buy'?'active':''}">买入</button><button data-side="sell" class="${state.side==='sell'?'active':''}">卖出</button></div><form id="order-form"><div class="field-top"><label for="amount">支付数量</label><span class="muted">演示余额</span></div><div class="amount-field"><input id="amount" type="number" min="0.00000001" max="1000000" step="any" placeholder="0.00" required><b>${state.side==='buy'?'ETH':m.symbol}</b></div><div class="quick-amount">${(state.side==='buy'?[.01,.05,.1,.5]:[100,500,1000,5000]).map(n=>`<button type="button" data-amount="${n}">${n}</button>`).join('')}</div><div class="swap-arrow">↓</div><div class="receive"><span class="muted">预计获得</span><strong id="receive">—</strong><span>${state.side==='buy'?m.symbol:'ETH'}</span></div><label class="slippage" for="slippage">滑点容忍度<select id="slippage"><option value="0.5">0.5%</option><option value="1">1%</option><option value="3">3%</option></select></label><div class="order-details"><p><span>市场阶段</span><b>${m.progress===100?'In Bloom':'Curve'}</b></p><p><span>${m.progress===100?'毕业池协议费':'演示费率（非链上报价）'}</span><b>1%</b></p><p><span>最少获得</span><b id="minimum">—</b></p></div><button class="button primary full" type="submit">预览${state.side==='buy'?'买入':'卖出'} ↗</button><p class="footnote">${m.progress===100?'毕业池 LP 协议手续费为 0；实际费用按收费资产记账。':'真实曲线报价须包含冻结基础费与开盘反狙击税。'} 当前使用固定示例价格估算，不发送交易。</p></form></div><div class="panel growth-panel"><div class="section-heading"><h3>${m.progress===100?'✳ 已绽放':'◌ 生长进度'}</h3><b>${m.progress}%</b></div><div class="progress"><i style="width:${m.progress}%"></i></div><p>${m.progress===100?'初始流动性永久锁定。现在可以分配 '+m.base+' STOCK，参与手续费分配。':'最后一笔曲线买入将原子完成毕业。绽放前不开放 STOCK 分配。'}</p><a href="${m.progress===100?'#rewards':'#markets'}">${m.progress===100?'查看 STOCK 奖励':'发现更多市场'} ↗</a></div></aside></div>`;}
function create(){return `${heading('PLANT SOMETHING ORIGINAL','你的灵感，下一个社区。','从一个 Ticker 开始，让志同道合的人聚在一起。')}<div class="create-grid"><form id="create-form" class="panel create-form"><div class="form-section"><span class="step-label">01 / TOKEN IDENTITY</span><h2>种下一个名字</h2><div class="form-row"><label>代币名称<input name="name" maxlength="32" placeholder="例如 NVIDIA CAT" required></label><label>代币符号<input name="symbol" maxlength="10" pattern="[A-Za-z][A-Za-z0-9]{1,9}" placeholder="例如 NVCAT" required></label></div><label>社区介绍<textarea name="description" maxlength="280" placeholder="这个社区相信什么？讲讲你的灵感。" rows="3" required></textarea></label><label>创建者收益地址（可选，默认创建者）<input name="beneficiary" pattern="0x[0-9a-fA-F]{40}" placeholder="0x…"></label><label>社区链接（可选）<input type="url" name="website" placeholder="https://"></label></div><div class="form-section"><span class="step-label">02 / GROWING FOUNDATION</span><h2>选择生长的土壤</h2><label>关联 STOCK<select name="base">${['NVDA','TSLA','AAPL','AMZN','META','COIN'].map(s=>`<option>${s}</option>`).join('')}</select></label><p class="field-help">本地示例目录。正式创建须读取 ACTIVE 注册资产；一经创建不可更改。仅在市场绽放后开放对应 STOCK 分配。</p><div class="form-row"><label>报价资产<select name="quote"><option>ETH</option></select></label><label>发行模板<input value="标准曲线 · 示例模板" disabled></label></div><p class="field-help">只允许已批准的报价资产。报价资产与关联 STOCK 分别选择。</p></div><div class="form-section"><span class="step-label">03 / FIRST SEED</span><h2>为社区种下第一笔</h2><label>创建者首买（可选，ETH）<input type="number" name="firstBuy" min="0" max="100" step="any" value="0"></label><p class="field-help">创建与首买可在同一笔链上交易完成。示例页面仅生成发行预览。</p><label class="checkbox"><input type="checkbox" required>我理解关联 STOCK 和报价配置在创建后不可更改。</label><button class="button primary full" type="submit">预览发行 <span>↗</span></button></div></form><aside><div class="panel preview-card"><div class="eyebrow">YOUR NEXT BIG IDEA</div><div class="preview-symbol" id="preview-icon">✳</div><h2 id="preview-name">你的代币名称</h2><p id="preview-ticker">$TICKER · NVDA 生态</p><span class="badge">◌ Ready to grow</span><hr><p id="preview-description">一个全新的社区，从这里开始。</p></div><div class="panel launch-summary"><h3>清晰发行，安心生长</h3><p><span>创建费</span><b>0.0005 ETH</b></p><p><span>创建者附加税</span><b>0%</b></p><p><span>首买</span><b id="preview-buy">0 ETH</b></p><p><span>发行方式</span><b>标准曲线</b></p><small>创建费不包含 Gas 与首买金额。曲线参数由批准模板冻结，不在此页面自定义。</small></div></aside></div>`;}
function analytics(){const factor=state.range==='7D'?1:state.range==='30D'?3.8:9.2;return `${heading('THE GARDEN, IN NUMBERS','每一份生长，都有迹可循。','追踪交易活力、社区规模与实际手续费分配。',`<div class="segmented">${['7D','30D','90D'].map(p=>`<button data-range="${p}" class="${state.range===p?'active':''}">${p}</button>`).join('')}</div>`)}${stats([['期间交易量',money(1840000*factor),'演示期间累计'],['期间协议手续费',money(18400*factor),'以 USD 估值参考'],['交易账户',Math.round(2864*factor).toLocaleString(),'演示去重账户数'],['新增市场',Math.round(6*factor),'演示期间新增']])}<div class="analytics-grid"><div class="panel bar-panel"><div class="section-heading"><div><h3>交易活力</h3><p class="muted">过去 ${state.range.replace('D','')} 天 · USD 参考</p></div><span class="legend"><i></i> 交易量</span></div><strong>${money(1840000*factor)}</strong><div class="bar-chart">${Array.from({length:28},(_,i)=>`<div style="height:${22+((i*19+Math.round(factor)*7)%63)}%" title="第 ${i+1} 组：${money((30000+i*3500)*factor)}"></div>`).join('')}</div><div class="x-axis"><span>期间开始</span><span>期间中点</span><span>期间结束</span></div></div><div class="panel fee-panel"><h3>每笔手续费，清晰分配</h3><p class="muted">毕业后 · 存在有效质押</p><div class="donut"><div><strong>1%</strong><span>毕业池总协议费</span></div></div><div class="fee-legend"><p><i style="background:#234c37"></i>创建者 <b>40%</b></p><p><i style="background:#c5ed79"></i>STOCK 质押者 <b>30%</b></p><p><i style="background:#deded2"></i>平台 <b>30%</b></p></div><small>曲线阶段或无有效质押：创建者 70% / 平台 30%。LP 协议手续费始终为 0。</small></div></div><div class="panel"><div class="section-heading padded"><h3>社区交易榜</h3><a href="#markets">探索市场 ↗</a></div>${table([...markets].sort((a,b)=>b.volume-a.volume).slice(0,4))}</div>`;}
function rewards(){return `${heading('YOUR HARVEST','参与生长，分享收获。','将对应 STOCK 分配到已绽放市场，分享真实产生的手续费。')}${stats([['可领取 · Quote','0.0248 ETH','演示已记账手续费'],['可领取 · Meme','12,480 NVCAT','按原资产独立领取'],['已分配 STOCK','12 NVDA','本金独立托管'],['有效仓位','1','仅有效份额参与分配']])}<div class="tabs reward-tabs">${[['stake','STOCK 奖励'],['creator','创建者收入'],['treasury','国库分配']].map(([k,t])=>`<button data-reward="${k}" class="${state.rewardTab===k?'active':''}">${t}</button>`).join('')}</div>${state.rewardTab==='stake'?`<div class="reward-grid"><div class="panel position-panel"><div class="section-heading"><h2>我的生长仓位</h2><span class="badge bloom">演示账户</span></div><div class="token-cell">${icon(markets[0])}<div><h3>NVIDIA CAT</h3><span class="muted">NVCAT · 关联 NVDA STOCK</span></div></div><div class="position-stats"><div><span>有效分配</span><strong>12 NVDA</strong></div><div><span>待激活</span><strong>0 NVDA</strong></div><div><span>锁定状态</span><strong>已解锁</strong></div></div><div class="reward-amounts"><p><span>Quote 奖励</span><b>0.0248 ETH</b></p><p><span>Meme 奖励</span><b>12,480 NVCAT</b></p></div><div class="button-row"><button class="button primary" data-action="claim">预览领取 ↗</button><button class="button outline" data-action="allocate">增加分配 ＋</button><button class="text-button" data-action="close">整仓退出</button></div><button class="emergency" data-action="ragequit">紧急退出说明 ↗</button></div><div class="panel reward-guide"><span class="eyebrow">HOW HARVEST WORKS</span><h2>收获来自交易，<br>而非增发。</h2><ol><li><b>选择 In Bloom 市场</b><p>只能分配市场绑定的同一种 STOCK。</p></li><li><b>30 秒延迟激活</b><p>新增份额激活后开始计奖；不追溯历史收益。</p></li><li><b>24 小时仓位锁定</b><p>从最新一次分配起算。解锁后可领取或整仓退出。</p></li></ol></div></div><div class="panel vault-strip"><div><h3>我的 STOCK 金库</h3><p class="muted">空闲余额不产生市场手续费收益</p></div><div><span class="muted">NVDA 总存入</span><b>20 NVDA</b></div><div><span class="muted">空闲余额</span><b>8 NVDA</b></div><button class="button outline" data-action="deposit">存入</button><button class="button outline" data-action="withdraw">提取空闲余额</button></div>`:state.rewardTab==='creator'?`<div class="panel spacious"><div class="eyebrow">CREATOR REVENUE</div><h2>社区的成长，也属于创造者。</h2><p>曲线阶段创建者获得手续费的 70%；毕业后有有效质押时为 40%，否则为 70%。</p><div class="reward-amounts"><p>待领取 ETH <b>0.0162 ETH（演示）</b></p><p>待领取 NVCAT <b>8,600 NVCAT（演示）</b></p></div><button class="button primary" data-action="creator">预览领取创建者收入 ↗</button></div>`:`<div class="panel spacious"><div class="eyebrow">TREASURY DISTRIBUTIONS</div><h2>独立的国库分配</h2><p>国库分配与市场质押奖励独立，不是持有 STOCK 即自动获得的收益。</p><div class="epoch-flow"><span>待请求</span><span>已请求</span><span>根待确认</span><span>领取中</span><span>已结转</span></div><p class="field-help">按市场的 30 天 Quote 轮次独立分配。请求需要服务费，过期请求可产生退款额度；领取必须有有效证明。</p><div class="empty">暂无可验证的分配证明<br><small>连接真实数据后，将按账户、资产与有效分配轮次展示可领取记录。</small></div><button class="button outline" data-action="treasury">查看领取条件</button></div>`}`;}
function render(){const parts=location.hash.slice(1).split('/');let page=parts[0]||'home';if(!nav.some(n=>n[0]===page))page='home';if(page==='trade'&&markets.some(m=>m.id===parts[1]))state.selected=parts[1];document.title=`${nav.find(n=>n[0]===page)[1]} · TickerGarden V2`;$('#app').innerHTML=`<header><a href="#home" class="brand"><span class="brand-icon">✳</span>TickerGarden<span class="version">V2</span></a><nav aria-label="主导航">${nav.map(([id,t])=>`<a href="#${id}" class="${page===id?'active':''}" ${page===id?'aria-current="page"':''}>${t}</a>`).join('')}</nav><button id="wallet" class="wallet">${state.wallet?'◉ 演示账户':'连接钱包 ↗'}</button></header><div class="demo-bar"><span><i class="live-dot"></i> 交互演示 · 所有行情、余额与交易均为模拟数据</span><span>ROBINHOOD CHAIN ECOSYSTEM ↗</span></div><main>${({home,markets:marketPage,trade,create,analytics,rewards}[page])()}</main><footer><a class="brand" href="#home"><span class="brand-icon">✳</span>TickerGarden</a><span>Culture takes root. Communities bloom.</span><span>V2 独立前端 · 2026</span></footer>`;bind();}
function toast(text){$('#toast').textContent=text;$('#toast').classList.add('show');clearTimeout(state.toast);state.toast=setTimeout(()=>$('#toast').classList.remove('show'),3000);}
function modal(title,body,button='了解了',callback){const d=$('#modal');d.innerHTML=`<button class="dialog-close" aria-label="关闭">×</button><div class="eyebrow">TICKERGARDEN · DEMO</div><h2>${title}</h2>${body}<button class="button primary full" id="dialog-confirm">${button}</button>`;d.querySelector('.dialog-close').onclick=()=>d.close();d.querySelector('#dialog-confirm').onclick=()=>{d.close();callback?.();};d.showModal();}
function updateOrder(){const amount=Number($('#amount').value),slip=Number($('#slippage').value),e=estimate(amount,token().price,state.side,slip);$('#receive').textContent=e?e.output.toLocaleString('en-US',{maximumFractionDigits:6}):'—';$('#minimum').textContent=e?e.min.toLocaleString('en-US',{maximumFractionDigits:6}):'—';}
function bind(){ $('#wallet').onclick=()=>modal('体验你的链上花园','<p>当前版本提供本地交互演示。启用演示账户后可以体验操作预览；不会连接钱包、请求签名或提交链上交易。</p>','进入演示账户',()=>{state.wallet=true;render();toast('已启用演示账户');});
 document.querySelectorAll('[data-favorite]').forEach(b=>b.onclick=()=>{const id=b.dataset.favorite;state.favorites.has(id)?state.favorites.delete(id):state.favorites.add(id);localStorage.setItem('tg-v2-favorites',JSON.stringify([...state.favorites]));render();});
 const bindings=[['filter','filter'],['period','period'],['side','side'],['range','range'],['reward','rewardTab']];for(const [attr,key] of bindings)document.querySelectorAll(`[data-${attr}]`).forEach(b=>b.onclick=()=>{state[key]=b.dataset[attr];render();});
 if($('#market-search'))$('#market-search').oninput=e=>{const pos=e.target.selectionStart;state.query=e.target.value;render();$('#market-search').focus();$('#market-search').setSelectionRange(pos,pos);};if($('#market-sort'))$('#market-sort').onchange=e=>{state.sort=e.target.value;render();};if($('#trade-market'))$('#trade-market').onchange=e=>{location.hash='trade/'+e.target.value;};
 if($('#amount')){$('#amount').oninput=updateOrder;$('#slippage').onchange=updateOrder;document.querySelectorAll('[data-amount]').forEach(b=>b.onclick=()=>{$('#amount').value=b.dataset.amount;updateOrder();});$('#order-form').onsubmit=e=>{e.preventDefault();const out=estimate(Number($('#amount').value),token().price,state.side,Number($('#slippage').value));if(!out)return;modal('订单预览',`<p>${state.side==='buy'?'买入':'卖出'} ${esc(token().symbol)}</p><div class="review-line"><span>支付</span><b>${esc($('#amount').value)} ${state.side==='buy'?'ETH':token().symbol}</b></div><div class="review-line"><span>预计获得</span><b>${esc($('#receive').textContent)} ${state.side==='buy'?token().symbol:'ETH'}</b></div><p class="notice">演示估算采用固定价格与示例费率，不包含真实价格冲击、Gas 或曲线反狙击税。没有提交交易。</p>`);};}
 if($('#create-form')){const f=$('#create-form');f.oninput=()=>{const d=new FormData(f);$('#preview-name').textContent=d.get('name')||'你的代币名称';$('#preview-ticker').textContent='$'+(d.get('symbol')||'TICKER').toUpperCase()+' · '+d.get('base')+' 生态';$('#preview-description').textContent=d.get('description')||'一个全新的社区，从这里开始。';$('#preview-buy').textContent=(d.get('firstBuy')||0)+' ETH';};f.onsubmit=e=>{e.preventDefault();const d=new FormData(f);modal('发行预览',`<h3>${esc(d.get('name'))} · $${esc(d.get('symbol').toUpperCase())}</h3><p>${esc(d.get('description'))}</p><div class="review-line"><span>关联 STOCK</span><b>${esc(d.get('base'))}</b></div><div class="review-line"><span>报价资产</span><b>ETH</b></div><div class="review-line"><span>创建费 + 首买</span><b>${(0.0005+Number(d.get('firstBuy'))).toFixed(6)} ETH</b></div><p class="notice">这是发行草稿。尚未接入 ACTIVE 注册目录、部署地址和钱包，未创建任何链上代币。</p>`,'保存本地草稿',()=>{localStorage.setItem('tg-v2-draft',JSON.stringify(Object.fromEntries(d)));toast('发行草稿已保存到此浏览器');});};const saved=localStorage.getItem('tg-v2-draft');if(saved){try{for(const [k,v]of Object.entries(JSON.parse(saved))){if(f.elements[k]&&f.elements[k].type!=='checkbox')f.elements[k].value=v;}f.oninput();}catch{}}}
 document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>{const act=b.dataset.action;const content={claim:['领取预览','可领取 0.0248 ETH 与 12,480 NVCAT，按原资产分别结算。演示仓位已解锁。'],allocate:['增加分配','示例空闲余额为 8 NVDA。增仓会重置整个仓位的 24 小时锁定；新增份额 30 秒后激活，原有效份额继续计奖。'],close:['整仓退出预览','解锁后整仓关闭，12 NVDA 返回金库空闲余额，并结算双资产收益。不支持部分减仓。'],ragequit:['紧急退出：本金优先','可绕过 24 小时锁，整仓返还 12 NVDA 到本人钱包，但放弃该仓位全部未领取 ETH 与 NVCAT 奖励。奖励清理完成前不能重入同一仓位。'],deposit:['存入 STOCK','存入形成金库空闲余额，不等于向某市场分配，也不会自动产生奖励。'],withdraw:['提取空闲余额','本例可提取 8 NVDA 空闲余额。已分配本金须通过整仓关闭或紧急退出处理。'],creator:['创建者收入预览','示例收入：0.0162 ETH 与 8,600 NVCAT。按资产分别领取，不自动兑换。'],treasury:['国库领取条件','需要账户对应的有效分配轮次、资产与可验证证明；此演示没有可领取凭证。']}[act];modal(content[0],`<p>${content[1]}</p><p class="notice">当前仅展示操作预览，没有提交链上交易。</p>`);});}
window.addEventListener('hashchange',()=>{render();window.scrollTo(0,0);});render();
