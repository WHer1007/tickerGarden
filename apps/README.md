# Applications

Runnable user-facing applications live here.

| Path | Responsibility |
| --- | --- |
| `web/` | Canonical TickerGarden V1 browser application and legal pages |

Backend processes are grouped separately under [`services/`](../services/README.md). The archived legacy frontend remains under `archive/` and is not an active application.
