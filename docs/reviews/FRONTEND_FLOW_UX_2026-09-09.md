# 前端全流程体验检查与修复 — 2026-09-09

范围：test 分支的创建代币、交易、毕业状态、Stock 质押、质押收益、创作者收益和持有人收益。方法为代码链路审查、前端自动化测试，以及本地页面的桌面/手机浏览器检查。

## 逐项发现与修复

| 顺序 | 流程 / 原有缺口 | 本次处理 | 验证状态 |
|---|---|---|---|
| 1 | 创建：填写期间刷新会丢失表单 | 按链保存本机草稿，恢复名称、符号、链接、金额及开关；图片仅记录是否曾选择，刷新后要求重选或明确放弃。支持清空草稿，成功创建与恢复成功后清除草稿 | 草稿读写/隔离/异常测试通过；浏览器实际填表、切换开关、刷新恢复和清空通过 |
| 2 | 创建：旧草稿资产配置可能过期 | 不恢复派生协议配置；已失效的配对/质押资产要求重新选择，不静默替换；清空时同步清除字段错误 | 类型检查、代码核对；失效资产分支未在真实配置中触发 |
| 3 | 交易：报价过期后按钮状态和下一步不够明确 | 报价到期主动清除并提供 Refresh quote；已有余额不足时禁用提交并提示；毕业阶段与合约路由标志必须一致 | 路由状态测试通过；真实报价到期与余额不足签名前流程待有市场联调 |
| 4 | 交易/毕业：直接进入空详情页像持续加载；外部交易触发毕业后状态可能滞后 | 明确选择市场/加载失败状态，增加 Browse tokens；无图片使用中性占位图；可见详情页每 30 秒读取当前市场，阶段变化后刷新到对应池，无毕业蒙层 | 空入口浏览器检查、路由测试通过；链上阶段翻转待真实毕业验证 |
| 5 | 全局交易：短暂 toast 容易错过，刷新后容易重复提交 | 增加页内交易阶段、交易浏览器入口；待确认记录阻止新提交；刷新恢复区区分授权与业务交易；损坏的记录不再导致页面异常，也不会自动删除或重发 | 交易执行器测试通过；损坏记录新增回归覆盖；本轮未广播 |
| 6 | Stock 质押：金额错误和退出后果不够集中 | 区分精度、余额、最小仓位错误；成功后清空金额；加仓重置整仓 24h 锁定、紧急退出放弃未领收益均增加页内确认 | 类型/现有交易测试通过；真实质押及到账待钱包联调 |
| 7 | 质押收益：需要回 Positions 选择市场；禁用原因不明确 | 独立市场选择并与仓位同步；区分锁定、已结算可领、待兑换、紧急退出后清理、暂无收益；主卡展示正常代币单位 | 分支单元测试通过；桌面/手机入口实查通过；有仓位领取待链上验证 |
| 8 | 创作者收益：epoch 容易被误解为奖励周期；收款对象容易混淆 | 改为收款人版本，提供使用当前版本按钮；切换市场重置版本；显示正常单位金额及收款人；转移收款人、原币领取放入高级区域并说明影响 | 类型/现有测试通过；手机入口检查通过；历史版本和实际到账待链上验证 |
| 9 | 持有人收益：主界面暴露大量 Root、证明和维护细节 | 首先显示奖励周期、状态和领取入口；证明、分发维护、退款折叠；区分已领、到期、等待分发等提示；按绑定合约保留周期分发与连续释放两类实际模式 | 类型/现有测试通过；浏览器确认高级区默认折叠、390px 无横向溢出；真实 eligibility/到账待链上验证 |
| 10 | 查询：进入收益页同时读取多个角色和应急路径 | 只刷新当前可见收益角色，合并重复刷新；直接 Vault 应急查询改为打开高级区域后读取；隐藏页面停止相关新增轮询 | 角色选择测试和代码核对通过；未做生产流量压测 |

## 验证结果

- `npm --prefix apps/web test`：221 项通过，包括生成 ABI/Read API 客户端一致性与 TypeScript 检查。
- `npm --prefix apps/web run build`：通过。仍有既有的大 bundle 提示，本轮未改依赖或拆包。
- `git diff --check`：通过。
- 实查本地 `/create`、`/trade`、`/claim` 各角色入口；1280px 桌面和 390px 手机；审查标签页无 JS error 日志。
- 草稿 QA 数据已通过页面内确认清除。

## 证据文件

截图位于本地 `outputs/reviews/frontend-flow-2026-09-09/`：

- `07-create-restored.png`：草稿恢复。
- `08-staker-after-desktop.png`：最终桌面质押收益入口。
- `09-creator-after-mobile.png`：手机端收益导航与连接钱包入口。
- `10-draft-confirm-mobile.png`：页内确认弹窗。
- `11-holder-after-mobile.png`：手机持有人收益说明。
- `12-trade-after-desktop.png`：最终空市场交易入口。

早期 `04-creator-before.png` 是浏览器整页拼接异常，不用于验收；`06-trade-before.png` 已包含当时热更新后的状态，不用于前后对照。

## 联调边界

当前审查浏览器未连接钱包，目录未加载任何市场。因此本次不能证明创建成功、买卖成交、链上毕业、质押及三类收益真实到账。以上主流程成功状态仍需使用有资产钱包和实际市场完成联调；这不要求先扫描历史区块。

本轮没有重新检查部署、扫描历史区块、广播交易、上传元数据或修改合约。test/master 的毕业金额、奖励周期、Anti-snipe 设置未变更。新增市场读取只针对打开的详情页，不是逐块同步；本报告不声称已经测得新的 RPC CU 峰值。
