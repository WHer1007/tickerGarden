[Unit]
Description=Repair and dispatch TickerGarden test chain queue
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
EnvironmentFile=/etc/tickergarden/test.env
ExecStart=/bin/sh -c 'exec /usr/bin/curl --fail --silent --show-error --max-time 120 -X POST -H "Authorization: Bearer $TG_REPAIR_TOKEN" https://tickergarden-pipeline-test.vercel.app/internal/dispatch'
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
