# Retired STOCK price/backing-target research

The following files are retained only as historical `V2-EXEC-1` research evidence:

- `generate_v2_backing_targets.py`
- `v2_backing_target_input.json`
- `generate_v2_chainlink_feed_directory.py`
- `v2_chainlink_feed_directory.snapshot.json`
- `v2_chainlink_feed_directory.source.json`

They are not inputs to the current `V2-EXEC-4`, are not imported by its current execution path, and must not be used to determine official STOCK eligibility, market creation, staking weight, fee distribution, implementation readiness, deployment readiness, or production readiness. Current `V2-EXEC-4` uses only official identity, token-safety checks, the Registry-configured raw-unit `minimumAllocation`, and raw STOCK balances for staking; the archived `V2-EXEC-3` evidence likewise did not use STOCK price inputs.

The raw source archives remain unchanged so that the earlier investigation is auditable.
